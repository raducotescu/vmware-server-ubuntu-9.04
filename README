The script will automatically patch and then run the install process for VMware
Server on Ubuntu machines.

To run the script either launch it from the same folder that contains the VMware
Server archive or supply it the path to this folder. Be careful to run the
script as a superuser.
